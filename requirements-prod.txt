# PyFinFlow - Dependencias de Producción (API FastAPI)
# Versión liviana para deploy en Render/Railway.
# Excluye deps opcionales pesadas (streamlit, chromadb, sentence-transformers,
# scikit-learn, plotly) que se importan de forma lazy y no se necesitan para
# que la API + IA cloud (HuggingFace/Gemini) funcionen.
# Python >= 3.10

# API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Authentication
bcrypt>=4.0.0
PyJWT>=2.8.0

# Environment variables
python-dotenv>=1.0.0

# Monitoring
structlog>=24.1.0

# Cache
redis>=5.0.0

# Email alerts
aiosmtplib>=3.0.0

# AI - cloud providers (imports lazy)
huggingface-hub>=0.20.0
google-generativeai>=0.3.0

# HTTP client (tests, refresh, webhooks)
httpx>=0.26.0
